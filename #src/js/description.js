import { addButton, button, clickButton } from "./_description.button";

window.addEventListener("load", addButton);
window.addEventListener("resize", addButton);

button.addEventListener("click", clickButton);
